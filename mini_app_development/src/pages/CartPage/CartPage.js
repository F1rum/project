export default function CartPage() {
    return (
        <div style={{ padding: "20px", color: "#fff" }}>
            <h1>Корзина</h1>
            {/* Вывод товаров из корзины */}
        </div>
    )
}
